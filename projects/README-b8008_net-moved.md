# b8008_net has moved

Extracted 2026-07-10 to its own repository: `~/Development/remote_8008`
(LiteX Etherbone monitor SoC). It consumes this repo's b8008 core via
FuseSoC (`greygiant:retro:b8008` — see `docs/fusesoc.md`). The Etherbone
plan doc and its design spec moved with it (this repo's copies were the
stale pre-extraction snapshots and are deleted; the living plan is in
remote_8008, which also holds the spec at the path its plan references).
